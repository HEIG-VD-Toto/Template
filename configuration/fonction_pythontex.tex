%% Configuration pour pythontex
\setpythontexlistingenv{pylisting}
\begin{pycode}
def QTY(var, unit):
        return '\\qty{' + str(var) + '}{' + unit + '}'
def NUM(var, unit):
    return '\\num{' + str(var) + '}{' + unit + '}'
\end{pycode}